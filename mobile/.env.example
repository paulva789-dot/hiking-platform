# Point this at your machine's LAN IP, not localhost — a physical device or
# emulator can't reach your dev machine's localhost the way a browser can.
# Find your IP with `ipconfig` (Windows) and use it here, e.g. 192.168.1.42.
EXPO_PUBLIC_API_URL=http://192.168.1.42:4000/api
